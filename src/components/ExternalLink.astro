---
interface Props {
  url: string;
  title: string;
}

const { url, title } = Astro.props;
---

<a
  class="text-white underline hover:text-green focus:outline-1 focus:outline-blue focus:outline-dashed"
  href={url}
  target="_blank"
  rel="noopener noreferrer">{title}</a
>
